# Historical expansion · 0.12.0

The repeated expansion pipeline has been superseded by the explicit layouts in [AUTHORED_DAYS.md](AUTHORED_DAYS.md). The `v0.12.0` Git tag preserves the prior layouts, measurements and generator for comparison. Current source builds use `tools/build_authored_stages.py`.
